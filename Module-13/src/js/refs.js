const searchFormRef = document.querySelector('#search-form');
const galleryRef = document.querySelector('#gallery-js');
const loadMoreBtnRef = document.querySelector(
  `button[data-action="load-more"]`,
);

export default {
  searchFormRef,
  galleryRef,
  loadMoreBtnRef,
};
